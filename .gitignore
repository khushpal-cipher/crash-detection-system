
# Auto-downloaded model & large weights
# Runtime outputs
# Virtual environments
# macOS
*.log
*.pyc
.DS_Store
__pycache__/
crash_env/
crash_outputs/
models/crash_detection_model.h5
models/crash_detection_model/
models/crash_model_cpu/
models/crash_model_saved/
models/feature_extractor.keras
myenv/
tf-env/
venv/
venv_orbstack/
videos
yolov8n.pt

# Nexar dataset videos (2.7 GB) - re-downloadable, never commit
data/nexar/**/*.mp4

# BADAS-Open weights (3.7 GB) - gated HF download, re-fetchable, never commit
models/badas/

# DADA-2000 gate 3a clips (1350 MiB) - rebuilt from Drive, never commit
data/dada2000/

# Colab staging bundle - derived, rebuild with scripts/make_colab_bundle.py
runs/colab_bundle.tar.gz

# Decoded-frame reference for the GATE C decode diagnostic (8 MiB, derived) -
# rebuild in seconds with scripts/export_pixel_reference.py
runs/pixel_ref/

# Kaggle probe staging folder (14 MB, derived) - rebuild with scripts/make_kaggle_probe.py
runs/kaggle_probe/
